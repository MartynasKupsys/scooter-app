import { ADD_RECORD } from "../Constants/constants";

function addReducer(state, action) {
  switch (action.type) {
    case ADD_RECORD:
      break;
    default:
  }

  return;
}

export default addReducer;
